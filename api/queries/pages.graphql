query GetPageByPath($path: [String!]!) {
  entry(path: $path) {
    ...ModularPageContent
  }
}

query GetDynamicPagePaths {
  taxonomy(filter: { path: ["website", "home"] }) {
    nodes {
      path
    }
  }
}
